transformers==5.15.0
tokenizers>=0.22.2
accelerate==1.14.0
datasets>=4.4.1
peft==0.19.1
bitsandbytes==0.49.1
safetensors>=0.8.0
huggingface-hub>=0.36.0
scikit-learn==1.9.0
pyyaml==6.0.3

# Optional: vLLM backend for fast evaluation. Pick the version matching your torch:
#   torch 2.11.0+cu130 -> vllm==0.22.0
#   torch 2.13.0+cu130 -> vllm==0.27.1
# Requires ninja-build for flashinfer JIT kernels.
# vllm==0.22.0
